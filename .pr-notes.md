# PR-body notes (uncommitted; fold into the PR description)

## Ledger artifact counts in commit `2ffff18`

The message of commit `2ffff18` ("Record the move-effect slice in the ledger
and bump to 0.0.22.0") opens with:

> Fifteen new sub-file artifacts and eight refreshed ones

Both numbers are wrong. Diffing `ledger/pokeemerald.json` across that commit
gives **20 new** sub-file artifacts and **11 refreshed** ones:

**New (20)**

* `data/battle_scripts_1.s` — `BattleScript_EffectAbsorb`, `…Charge`,
  `…Confuse`, `…DefenseCurl`, `…FocusEnergy`, `…MultiHit`, `…Paralyze`,
  `…PoisonHit`, `…Sonicboom`, `…Splash`, `…StatUp` (11)
* `src/battle_script_commands.c` — `Cmd_adjustsetdamage`,
  `Cmd_negativedamage`, `Cmd_setcharge`, `Cmd_setdefensecurlbit`,
  `Cmd_setfocusenergy`, `Cmd_setmultihitcounter`, `SetMoveEffect` (7)
* `src/battle_util.c` — `AtkCanceler_UnableToUseMove`,
  `DoBattlerEndTurnEffects` (2)

**Refreshed (11)**

`data/battle_ai_scripts.s#AI_CheckBadMove`,
`data/battle_scripts_1.s#BattleScript_EffectStatDown`,
`data/battle_scripts_1.s#gBattleScriptsForMoveEffects`,
`data/maps/Route103#Route103_SightTrainers`,
`src/battle_main.c#CreateNPCTrainerParty`,
`src/battle_main.c#GetWhoStrikesFirst`,
`src/battle_script_commands.c#ChangeStatBuffs`,
`src/battle_script_commands.c#Cmd_critcalc`,
`src/battle_script_commands.c#Cmd_damagecalc`,
`src/battle_script_commands.c#Cmd_seteffectwithchance`,
`src/battle_setup.c#DoTrainerBattle`.

The commit itself cannot be amended (it is already part of the branch's
history), and **no in-repo file repeats the wrong count** — checked by
grepping the whole tree for the figures; the error is confined to that one
commit message. Recording it here so the PR body carries the correction.
