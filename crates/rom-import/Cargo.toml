[package]
name = "rom-import"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true

[dependencies]
# The importer's output side. `pack-format` is an in-workspace crate, not a
# new external dependency; this crate takes no others (minimal-deps).
pack-format = { path = "../pack-format" }

[lints]
workspace = true
