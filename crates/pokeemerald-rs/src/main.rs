//! `pokeemerald-rs` binary entry point (I-1).
//!
//! Wires `platform` -> `engine` -> `battle` once those subsystems exist. Stub
//! `main` for now so the workspace builds; no subsystem is wired yet.

fn main() {}
