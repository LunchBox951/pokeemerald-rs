//! Pins the staged sibling's name, its exclusive creation, the retries a
//! host's real limits force, and the cleanup that abandons it.

mod shared;

mod creation_and_cleanup;
mod holds_and_names;
mod namespace_exhaustion;
mod narrowing;
mod non_utf8_names;
